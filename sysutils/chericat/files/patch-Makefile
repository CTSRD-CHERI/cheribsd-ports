--- Makefile.orig
+++ Makefile
@@ -7,9 +7,8 @@
 CFLAGS+=-Wall -Wcheri
 
 SRC_BASE?=/usr/src
-ARCH+=aarch64
-INC=-I/usr/include -I/usr/local/include -I$(DEPS) -I${SRC_BASE}/libexec/rtld-elf -I${SRC_BASE}/libexec/rtld-elf/${ARCH}
-LDFLAGS=-L/usr/lib -L/usr/local/lib 
+ARCH?=aarch64
+INC=-I$(DEPS) -I${SRC_BASE}/libexec/rtld-elf -I${SRC_BASE}/libexec/rtld-elf/${ARCH}
 
 .PHONY: all
 .PHONY: make_dir
